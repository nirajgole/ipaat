1. `python --version`
2. `py -3.14 -m venv venv`
3. `python -m pip install --upgrade pip setuptools wheel`
4. `pip install pandas numpy matplotlib seaborn scikit-learn ipykernel`
5. `pip list --not-required --format=freeze > requirements.txt`
